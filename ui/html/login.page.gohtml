{{template "base" .}}

{{define "title"}}Login{{end}}

{{define "body"}}
    <h1>Login</h1>
    <form action='/user/login' method='post'>
        {{template "csrf" .}}
        {{with .Form}}
            <div>
                <label for='email-input'>Email:</label>
                <input type='email' name='email' id='email-input' value='{{.Get "email"}}'>
            </div>
            <div>
                <label for='password-input'>Password:</label>
                <input type='password' name='password' id='password-input' value='{{.Get "password"}}'>
            </div>
            <div>
                <input type='submit' value='Login'>
            </div>
        {{end}}
    </form>
{{end}}